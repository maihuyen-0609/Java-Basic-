package Toantu;

public class Quanhe {
    public static void main(String[] args) {
        int a = 10;
        int b = 11;
        int c = 10;
        System.out.println("So sánh a có bằng b không ? " + (a == b) ); //false
        System.out.println("So sánh a có khác b không ? " + (a != b) ); //false
        System.out.println("So sánh a có lớn hơn b không ? " + (a > b) ); //false
        System.out.println("So sánh a có lớn hơn c không ? " + (a > c) ); //false
        System.out.println("So sánh a có lớn hơn c không ? " + (a >= c) ); //false

    }
}
